// Generated by CoffeeScript 1.9.3
(function() {
  var About, App, Classes, D, DefaultRoute, Header, Home, Inbox, Projects, React, Route, RouteHandler, Router, routes;

  React = require('react');

  D = React.DOM;

  Router = require('react-router');

  Route = React.createFactory(Router.Route);

  DefaultRoute = React.createFactory(Router.DefaultRoute);

  RouteHandler = React.createFactory(Router.RouteHandler);

  Header = require('./header');

  Home = require('./home');

  Classes = require('./classes');

  Projects = require('./projects');

  About = React.createClass({
    render: function() {
      return D.p({}, 'About');
    }
  });

  Inbox = React.createClass({
    render: function() {
      return D.p({}, 'Inbox');
    }
  });

  App = React.createClass({
    render: function() {
      return D.div({}, React.createElement(Header, {}), RouteHandler());
    }
  });

  routes = Route({
    path: '/',
    handler: App
  }, DefaultRoute({
    name: 'home',
    handler: Home
  }), Route({
    name: 'classes',
    path: 'classes',
    handler: Classes
  }), Route({
    name: 'projects',
    path: 'projects',
    handler: Projects
  }));

  Router.run(routes, Router.HashLocation, (function(_this) {
    return function(Root) {
      return React.render(React.createElement(Root), document.getElementById('app'));
    };
  })(this));

}).call(this);
